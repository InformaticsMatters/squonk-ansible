---
kind: Ingress
apiVersion: networking.k8s.io/v1beta1
metadata:
  name: website
  namespace: {{ sq_namespace }}
  annotations:
    ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-nginx-{{ sq_website_cert_issuer|lower }}"
spec:
  tls:
  - hosts:
    - {{ sq_website_hostname }}
    secretName: website-tls
  rules:
  - host: {{ sq_website_hostname }}
    http:
      paths:
      - path: /
        backend:
          serviceName: website
          servicePort: http
